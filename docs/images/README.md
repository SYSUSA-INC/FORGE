# Screenshot assets

Screenshot PNGs referenced by the manuals live in this folder. See `docs/README.md` for the list of expected filenames.

Missing images render as a broken-image icon in GitHub markdown — they don't break the docs. Add them as you capture each one.
